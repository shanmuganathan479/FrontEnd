import React from 'react'

 export function NamedExp() {
  return (
    <div>
        <h1>This is Named export </h1>
        <p>We can use only this name to import</p>
        <p>For default export we cam use any name to import</p>
    </div>
  )
}

